<%- include('../partials/head') %>
<%- include('../partials/header') %>

<body>
    <div class="container">
        <ul>
            <% products.forEach((products)=> { %>
                <li>
                    <h2>
                        <%= products.title %>
                    </h2>
                    <p>
                        <%= products.body %>
                    </p>
                </li>
                <hr />
            <% }) %>
        </ul>
    </div>
</body>

<%- include('../partials/footer') %>